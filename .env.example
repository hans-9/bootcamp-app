# Copy to .env for local overrides: cp .env.example .env
# This app reads no secrets — the only runtime variable is the port.

# Port the Express server listens on. Defaults to 3001 if unset.
# On Render (and most hosts) this is injected automatically — do NOT hardcode it
# in production; the server already reads process.env.PORT.
PORT=3001
